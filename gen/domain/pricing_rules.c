/* AUTO-GENERATED by clipsgen 1.1.0 */
/* @generated Fri Mar  6 01:18:02 2026
 */

#include "pricing_rules.h"

/* Rule: ApplyGoldDiscount */
int pricing_rule_ApplyGoldDiscount(pricing_ctx_t *ctx) {
    /* Conditions (AND-ed) */
    if (!(1
        && (ctx->order_total > 100)
        && (ctx->customer_tier == 2))) return 0;

    /* Actions */
    ctx->discount = 0.15;
    return 1;
}

/* Rule: ApplySilverDiscount */
int pricing_rule_ApplySilverDiscount(pricing_ctx_t *ctx) {
    /* Conditions (AND-ed) */
    if (!(1
        && (ctx->order_total > 50)
        && (ctx->customer_tier == 1))) return 0;

    /* Actions */
    ctx->discount = 0.10;
    return 1;
}

/* Rule: FreeShipping */
int pricing_rule_FreeShipping(pricing_ctx_t *ctx) {
    /* Conditions (AND-ed) */
    if (!(1
        && (ctx->order_total > 75))) return 0;

    /* Actions */
    ctx->shipping_cost = 0;
    return 1;
}

int pricing_evaluate_all(pricing_ctx_t *ctx) {
    int fired = 0;
    fired += pricing_rule_ApplyGoldDiscount(ctx);
    fired += pricing_rule_ApplySilverDiscount(ctx);
    fired += pricing_rule_FreeShipping(ctx);
    return fired;
}
