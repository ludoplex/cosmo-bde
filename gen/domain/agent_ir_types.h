/* AUTO-GENERATED by schemagen 2.0.0 — DO NOT EDIT */
#ifndef agent_ir
#define agent_ir

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct AgentIrOp AgentIrOp;
typedef struct AgentIrBlock AgentIrBlock;
typedef struct AgentIrFunction AgentIrFunction;
typedef struct VexIrMapping VexIrMapping;
typedef struct AgentIrLiftRequest AgentIrLiftRequest;
typedef struct AgentIrLiftResult AgentIrLiftResult;

struct AgentIrOp {
    uint32_t id;
    uint8_t opcode;
    uint8_t origin;
    uint8_t operand_count;
    char result_name[64];
    char operand_a[128];
    char operand_b[128];
    char guard[256];
    char metadata[256];
};

struct AgentIrBlock {
    uint32_t id;
    char label[128];
    uint32_t op_count;
    int32_t is_entry;
    int32_t is_exit;
    uint32_t fallthrough;
    uint32_t cfg_node_id;
};

struct AgentIrFunction {
    char name[128];
    uint8_t framework;
    uint8_t origin;
    uint32_t block_count;
    uint32_t op_count;
    int32_t has_recursion;
    int32_t has_indirect;
    uint32_t entry_block_id;
    char module_name[128];
};

struct VexIrMapping {
    char vex_tag[32];
    uint8_t agent_opcode;
    char binary_example[128];
    char agent_example[128];
    char doc[256];
};

struct AgentIrLiftRequest {
    char source_name[128];
    char source_path[256];
    uint8_t framework;
    uint8_t origin;
    int32_t lift_cfg;
    int32_t lift_tah;
};

struct AgentIrLiftResult {
    char source_name[128];
    int32_t status;
    uint32_t function_count;
    uint32_t block_count;
    uint32_t op_count;
    uint64_t lift_time_us;
    char error_msg[256];
};

/* AgentIrOp functions */
void AgentIrOp_init(AgentIrOp *obj);
bool AgentIrOp_validate(const AgentIrOp *obj);

/* AgentIrBlock functions */
void AgentIrBlock_init(AgentIrBlock *obj);
bool AgentIrBlock_validate(const AgentIrBlock *obj);

/* AgentIrFunction functions */
void AgentIrFunction_init(AgentIrFunction *obj);
bool AgentIrFunction_validate(const AgentIrFunction *obj);

/* VexIrMapping functions */
void VexIrMapping_init(VexIrMapping *obj);
bool VexIrMapping_validate(const VexIrMapping *obj);

/* AgentIrLiftRequest functions */
void AgentIrLiftRequest_init(AgentIrLiftRequest *obj);
bool AgentIrLiftRequest_validate(const AgentIrLiftRequest *obj);

/* AgentIrLiftResult functions */
void AgentIrLiftResult_init(AgentIrLiftResult *obj);
bool AgentIrLiftResult_validate(const AgentIrLiftResult *obj);

#endif /* agent_ir */
