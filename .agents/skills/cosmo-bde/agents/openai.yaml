interface:
  display_name: "Cosmo Bde"
  short_description: "Repo-specific patterns and workflows for cosmo-bde"
  default_prompt: "Use the cosmo-bde repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true